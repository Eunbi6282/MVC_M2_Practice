

쿠키는 생성 시점에서는 값을 불러올 수 가 없다. 그 다음 페이지에서 쿠키 정보의 값을 불러올 수 있다.

쿠키 정보를 삭제 후 정보를 불러올 때 NullPointException 이 발생될 수 있다.

쿠키를 사용한 팝업
	- JavaScript 의 Windows.open 을 사용 < == 옛날, 브라우저에서 모두 차단 되어있음
	- 레이어를 사용하는 팝업처리 <== 근래엔 레이어를 많이 이용